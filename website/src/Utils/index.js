import { mutation } from "./Database";
import { decideMap, populateMap, getIcon } from "./Map";
import {
  handleLocalStorage,
  processData,
  checkIfUpdatedWeek,
  checkIfUpdatedChallenge,
  toggleAll
} from "./Season.js";
export {
  mutation,
  decideMap,
  populateMap,
  getIcon,
  handleLocalStorage,
  processData,
  checkIfUpdatedWeek,
  checkIfUpdatedChallenge,
  toggleAll
};
